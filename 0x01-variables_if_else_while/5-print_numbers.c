#include<stdio.h>

/**
 * main - program that prints all single digit numbers of base 10
 *
 * Return: print all single digit number staring from 0
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
