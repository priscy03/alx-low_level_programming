#include<stdio.h>
/**
 * main -program that prints the alphabet in lowercase ff by new line
 *
 * Return: Lower case alphabet
 */
int main(void)
{
char i;
for (i = 'a'; i <= 'z'; i++)
	putchar (i);
putchar ('\n');
	return (0);
}
