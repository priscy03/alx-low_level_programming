#include <stdio.h>
/**
 * main- A c program that prints exactly a line with printf
 *
 * Return: 0 when successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
